<head>
    <title>Flashcards</title>
    <meta charset="utf-8">
    <link href="../css/datepicker.css" rel="stylesheet">
    <link rel="stylesheet" href="css/selecciona.css" />
    <script src="js/jquery-1.11.3.js" type="text/javascript"></script>
    <script src="js/grupo.js"></script>
    <link rel="stylesheet" href="css/grupos.css" />
    <link href='https://fonts.googleapis.com/css?family=Ubuntu:500italic' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Oswald:700' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Abel' rel='stylesheet' type='text/css'>
    <script src="js/jquery-1.11.3.js" type="text/javascript"></script>
</head>
<body>
    <div align=left id="header">
        <img src="img/small-logo.png"/> <label id="title">CEREBRATON</label>
        <label id="nusuario"> Bienvenido <%= usuario %> </label>
    </div>
    <div id="menu">
        <table id="lista">
        <td class="seccion"><a href="entrada">Inicio</a></td>
        <td class="seccion"><a href="perfilProfesor.ejs">Perfil</a></td>
        <td class="seccion seleccionado"><a href="grupos">Grupos</a></td>
        <td class="seccion"><a href="agregarMaterial">Materiales</a></td>
        <td class="seccion"><a href="estadisticas">Estadisticas</a></td>
        </table>
    </div>
    <div id="body" align=center>
        <div id="Tituloseccion"> Grupo - Registrar Grupo </div>
        <div class="forma" id="inputNombre">
        Nombre: <input type="text" id="nombre" maxlength="125"/><br />
        </div>
        <div class="forma" id="inputDescripcion">
        Descripci&oacute;n:<textarea id="descrip" rows="5"></textarea><br />
        </div>
        <div class="forma" id="inputClave">
        Clave de acceso: <input type="text" id="clave" maxlength="25"/><br />
        </div>
        <div class="forma" id="inputFechaIn">
        Fecha de inicio: <input id="inicio" /><br />
        </div>
        <div class="forma" id="inputFechaFin">
        Fecha de fin: <input id="fin" /><br />
        <input type="submit" value="Registrar" id="alta" />
        </div>
    </div>
</body>
<script src="../js/bootstrap-datepicker.js"></script>
    <script>
    if (top.location != location) {
    top.location.href = document.location.href ;
  }
        $(function(){
            window.prettyPrint && prettyPrint();
            $('#inicio').datepicker({
                format: 'yyyy-mm-dd'
            });

            $('#fin').datepicker({
                format: 'yyyy-mm-dd'
            });
            
            var startDate = new Date(2012,1,20);
            var endDate = new Date(2012,1,25);
            
        // disabling dates
        var nowTemp = new Date();
        var now = new Date(nowTemp.getFullYear(), nowTemp.getMonth(), nowTemp.getDate(), 0, 0, 0, 0);

        var checkin = $('#dpd1').datepicker({
          onRender: function(date) {
            return date.valueOf() < now.valueOf() ? 'disabled' : '';
          }
        }).on('changeDate', function(ev) {
          if (ev.date.valueOf() > checkout.date.valueOf()) {
            var newDate = new Date(ev.date)
            newDate.setDate(newDate.getDate() + 1);
            checkout.setValue(newDate);
          }
          checkin.hide();
          $('#dpd2')[0].focus();
        }).data('datepicker');
        var checkout = $('#dpd2').datepicker({
          onRender: function(date) {
            return date.valueOf() <= checkin.date.valueOf() ? 'disabled' : '';
          }
        }).on('changeDate', function(ev) {
          checkout.hide();
        }).data('datepicker');
        });
    </script>
    <script src="http://www.google-analytics.com/urchin.js" type="text/javascript">
</script>
<script type="text/javascript">
_uacct = "UA-106117-1";
urchinTracker();
</script>
</footer>
